apiVersion: v1
kind: Service
metadata:
  name: dm-service
  labels:
    app: dm
spec:
  type: NodePort
  selector:
    app: dm
  ports:
    - port: 80
      targetPort: 80
      nodePort: 32085     # pick from 30000–32767
      protocol: TCP
